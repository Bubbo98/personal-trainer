# JWT Secret - GENERATE A STRONG SECRET KEY!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database
DB_PATH=./database/app.db

# Server Configuration
PORT=3001
NODE_ENV=development

# Admin Credentials - CHANGE THESE IN PRODUCTION!
# Use strong passwords with uppercase, lowercase, numbers and symbols
ADMIN_USERNAME=joshua_admin
ADMIN_PASSWORD=change-this-to-strong-password

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Vercel Analytics Configuration
# Get your token from: https://vercel.com/account/tokens
# Get your project ID from Vercel project settings
VERCEL_TOKEN=your-vercel-api-token-here
VERCEL_PROJECT_ID=your-vercel-project-id-here
# Optional: If using a Vercel team
# VERCEL_TEAM_ID=your-team-id-here